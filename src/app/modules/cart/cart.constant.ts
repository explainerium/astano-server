/** Cookie holding an anonymous visitor's cart token. */
export const CART_COOKIE = "astano_cart"

/** Anonymous carts are swept after this long without activity. */
export const GUEST_CART_TTL_DAYS = 30
